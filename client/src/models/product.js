export default class Product {
  constructor(title, desc, subtitle, img, categories, size, geography, price) {
    this.title = title;
    this.desc = desc;
    this.subtitle = subtitle;
    this.img = img;
    this.categories = categories;
    this.size = size;
    this.geography= geography;
    this.price = price;
  }
}