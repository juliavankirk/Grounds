export default class User {
  constructor(username, forename, surname, email, password) {
    this.username = username;
    this.forename = forename;
    this.surname = surname;
    this.email = email;
    this.password = password;
  }
}